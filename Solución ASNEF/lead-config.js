window.LEAD_CONFIG = {
  // Sustituye esta URL por tu endpoint real de formulario.
  // Por defecto el envío usa FormData (recomendado para Formspree).
  // Si tu API exige JSON: useJsonBody: true
  endpoint: 'https://formspree.io/f/mojpalwl',
  method: 'POST',
  headers: {
    Accept: 'application/json',
  },
  // useJsonBody: false,
};
